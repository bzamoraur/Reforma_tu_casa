# Reforma Quest Madrid — environment example
#
# The MVP has NO backend and requires NO secrets to run.
# This file documents the (currently empty) configuration surface only.
# Never commit a real .env file or any API keys (see docs/safety-policy.md).
#
# Optional, non-secret build-time flags (Vite exposes only VITE_*-prefixed vars):
# VITE_APP_ENV=development
